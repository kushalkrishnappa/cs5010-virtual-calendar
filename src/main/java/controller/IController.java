package controller;

/**
 * This class represents an interface for the controller of the calendar application. It defines a
 * single method `run()` that is responsible for starting the program.
 */
public interface IController {

  /**
   * Method to start the program. This method is responsible for gathering or initializing the model
   * and view components, and starting the application.
   */
  void run();
}
